export interface SidebarProps {
    isOpen: boolean;
    onClose: () => void;
  }
  
  export interface NationalHeaderProps {
    title: string;
  }
  
  export interface IconProps {
    size?: number;
    color?: string;
  }